<Window x:Class="DesktopPortal.Screens.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        DataContext="DesktopPortal.Screens.SplashScreen"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="400" Height="300"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Topmost="True">
    <Border Background="White" CornerRadius="12" BorderBrush="#8A153800" BorderThickness="2" Padding="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image x:Name="AppIcon" Source="/icon.ico" Width="100" Height="100" Margin="0,0,0,20" />
            <TextBlock Text="Desktop Portal" FontSize="26" FontWeight="Bold" Foreground="#8A1538" Margin="0,0,0,20" VerticalAlignment="Center" HorizontalAlignment="Center" />
            <TextBlock Text="Loading application..." FontSize="14" Foreground="Gray" Margin="0,0,0,5" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <ProgressBar IsIndeterminate="True" Height="5" Width="300" />
        </StackPanel>
    </Border>
</Window>
